{
  "name": "black-hole-gpu",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot server.ts",
    "start": "bun server.ts",
    "build": "bun build ./index.html --outdir dist --minify",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lil-gui": "^0.21.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@webgpu/types": "^0.1.74",
    "typescript": "^5"
  }
}
