{
  "name": "black-hole-gpu",
  "devDependencies": {
    "@types/bun": "latest",
    "@webgpu/types": "^0.1.74",
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "dev": "bun --hot server.ts",
    "start": "bun server.ts",
    "build": "bun build ./index.html --outdir dist --minify",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "type": "module"
}
